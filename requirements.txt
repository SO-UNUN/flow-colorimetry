# Flow Cell Colorimetry — Web App Dependencies
# University of Phayao | Physics Department
# ==============================================================
# Tested with Python 3.10–3.13
# Last updated: May 2026
# ==============================================================

# Web framework
streamlit~=1.57.0

# Video & image processing
opencv-python-headless~=4.10.0
Pillow~=10.4.0

# Numerical & data
numpy~=1.26.0
pandas~=2.2.0
scipy~=1.13.0

# Plotting (interactive)
plotly~=5.22.0

# ROI selection (drawable canvas)
# NOTE: ใช้ "streamlit-drawable-canvas-fix" แทนตัว original
# เพราะ original ไม่ compatible กับ Streamlit ≥ 1.40 แล้ว (image_to_url removed)
# fix package ใช้ API เดียวกัน — import ใช้งานเหมือนเดิม
streamlit-drawable-canvas-fix~=0.9.8

# Optional: scikit-image for advanced features
#scikit-image~=0.24.0

# ==============================================================
# Installation:
#   pip install -r requirements.txt
#
# ถ้าเคยติดตั้ง streamlit-drawable-canvas (ตัวเก่า) ไว้ ให้ uninstall ก่อน:
#   pip uninstall streamlit-drawable-canvas
#   pip install -r requirements.txt
# ==============================================================
